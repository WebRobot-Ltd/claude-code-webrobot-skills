apiVersion: v1
kind: Service
metadata:
  name: webrobot-mcp-service
  namespace: webrobot
  labels:
    app: webrobot-mcp
    service: mcp
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: webrobot-mcp
  sessionAffinity: None
