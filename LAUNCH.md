# Launch & growth assets — claude-code-webrobot-skills

Hero #1 (Claude Code / MCP audience). Goal: real GitHub stars + plugin installs.
Do the **repo hygiene** first, then **submit to registries**, then **launch**.

## 1. Repo hygiene (set in GitHub → About / Settings)
**Description:**
> Turn Claude Code into a data engineer — WebRobot Claude Code plugin + MCP server for Spark-native, BYOC web-ETL. Scrape, build pipelines, run them by conversation.

**Topics:**
`mcp` · `model-context-protocol` · `claude` · `claude-code` · `ai-agents` ·
`etl` · `web-scraping` · `data-engineering` · `apache-spark` · `llm` ·
`agentic` · `byoc`

- Add a **Social preview** image (Settings → Social preview, 1280×640).
- Pin the repo on the org profile; add an org profile README pointing here.

## 2. Submit to registries / lists (highest leverage for us)
- **MCP registries:** mcp.so · Smithery · `awesome-mcp-servers` (PR) · Cursor/Cline MCP directories.
- **Claude:** `awesome-claude` / `awesome-claude-code` (PR) · Claude Code plugin marketplace listing.
- **ETL / scraping:** `awesome-etl`, `awesome-web-scraping`, `awesome-selfhosted` (BYOC/sovereign angle).
- Each PR: one-line + link + what it does (MCP server exposing a Spark-native ETL platform).

## 3. Show HN (post ~13:00–15:00 UTC, then reply to every comment in hour 1)
**Title:** `Show HN: WebRobot – ETL for AI agents (Spark-native, BYOC, Claude Code plugin)`

**Body:**
> We built WebRobot, a Spark-native web-ETL platform that AI agents operate end to
> end — from a URL to an insight in a conversation. It runs on your own cloud (BYOC,
> EU-sovereign by design). This repo is the Claude Code plugin + an MCP server: 10
> skills + typed tools to scrape, build Spark pipelines and run them from Claude.
> Live demo: https://portal.webrobot.eu/chat.html · Docs: https://docs.webrobot.eu
> Happy to answer questions on the architecture (Spark on k8s, MCP, the plugin SDK).

Mirror to: r/dataengineering, r/LocalLLaMA, r/selfhosted; dev.to / Lobste.rs; X + LinkedIn (founder accounts).

## 4. Launch-day star clustering (organic, NOT bought)
Notify the list / Discord / founder networks in the **same hour** → star velocity →
GitHub Trending → compounding. Real users only.

## 5. Measure
GitHub API: stars-over-time, `/traffic/views`, `/traffic/popular/referrers`.
Track which channel converts; reallocate next week.
